/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ["i.pinimg.com", "www.notion.so", "i.ytimg.com"], // Agrega aquí el dominio de tu imagen externa
  },
};

export default nextConfig;
